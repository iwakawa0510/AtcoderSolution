#include <iostream>
#include <iomanip>
#include <algorithm>
#include <cmath>
#include <array>
#include <string>
#include <vector>
#include <set>
using namespace std;

int main(){ 
    //input,
    int n,m;
    cin >> n >> m;
    vector<int> s,t;
    for(int i = 0;i < n;i++)cin >> s[i];
    for(int i = 0;i < m;i++)cin >> t[i];

    //処理,
    for(int i = 0;i < n;i++){
        
    }
    return 0;
}