#include <iostream>
#include <vector>
#include <string>
using namespace std;

int main(){ 
    int N,S;
    cin >> N >> S;
    string A[N];
    for(int i = 0;i < N;i++){
        cin >> A[i]
    }
    string circle = A + A;
    
    if(circle.find(S) == )



    return 0;
}