#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main(){ 
    int Q;
    cin << Q;
    vector<int> q (3);
    for(int i =0;i < Q;i++){
        if(0 < q[0] < 4){

        }else{
            continue;
        }
    }
    return 0;
}