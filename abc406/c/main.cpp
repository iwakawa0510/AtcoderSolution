#include <iostream>
#include <iomanip>
#include <algorithm>
#include <cmath>
#include <array>
#include <string>
#include <vector>
#include <set>
using namespace std;

int main(){ 
    int n;
    cin >> n;
    vector<int> p;   
    for(int i = 0;i < n;i++){
        cin >> p[i];
    }
    int cnt;
    for(int i = 0;i < n;i++){
        // check tilde,
        for(int j = i;j < n-4;j++)
    }
    return 0;
}