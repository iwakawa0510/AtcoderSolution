#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main(){ 
    int N;
    vector<int> A;
    cin >> N;
    for(int i = 0;i < N;i++){
        cin >> A[i];
    }
    sort(A.begin(),A.end(),greater<int>());
    int i = 0;
    int cnt
    while (0 < N){
        A[i]
        
    }
    return 0;
}