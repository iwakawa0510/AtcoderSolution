#include <iostream>
#include <iomanip>
#include <algorithm>
#include <cmath>
#include <array>
#include <string>
#include <vector>
#include <set>
using namespace std;


int main(){ 
    int n,m;
    cin >> n >> m;

    vector<int> l,r(m);
    for(int i = 0;i < m;i++){
        cin >> l[i] >> r[i];
    }

    int ans = 0;
    vector<int> num(n,0);
    for(int i = 0;i < n;i++){
        w[i] = i;
    }

    for(int i = 0;i < m;i++)
    return 0;
}