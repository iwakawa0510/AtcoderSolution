#include <iostream>
#include <iomanip>
#include <algorithm>
#include <cmath>
#include <array>
#include <string>
#include <vector>
#include <set>
using namespace std;

int main(){ 
    int n;
    int q;
    vector<int> c(n);
    vector<int> p(n);
    cin >> n;
    for(int i = 0;i < n;i++){
        cin >> c[i] >> p[i];
    }
    cin >> q;
    for(int i = 0;i < q;i++){
        
    }
    return 0;
}