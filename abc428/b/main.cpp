#include <iostream>
#include <iomanip>
#include <algorithm>
#include <cmath>
#include <array>
#include <string>
#include <vector>
#include <set>
using namespace std;

int main(){ 
    int k,n;
    cin >> k >> n;
    string s (n);
    cin >> s;

    vector<string> ans;
    for(int i = 0;i < n;i++)
    return 0;
}